{
  "tasks": {
    "build": "pip install -r requirements.txt && pip install -r requirements-dev.txt && pre-commit install",
    "test": "pytest tests/ --cov=src --cov-report=term-missing",
    "launch": "python src/main.py"
  }
}